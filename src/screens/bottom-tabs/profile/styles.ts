import {StyleSheet} from 'react-native';

export const profileStyles = StyleSheet.create({
  container: {
    padding: 16,
    alignItems: 'center',
    justifyContent: 'center',
  },
  btn: {marginTop: 20, padding: 5, width: '50%'},
  mv10: {marginVertical: 10},
});
