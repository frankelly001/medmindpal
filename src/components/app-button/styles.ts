import {StyleSheet} from 'react-native';
import colors from '../../constants/colors';

export const appBtnStyles = StyleSheet.create({
  container: {
    paddingHorizontal: 30,
    paddingVertical: 20,
    borderRadius: 50,
    alignItems: 'center',
    justifyContent: 'center',
    flexDirection: 'row',
  },
});
