import {StyleSheet} from 'react-native';
import colors from '../../constants/colors';

export const appBackBtnStyles = StyleSheet.create({
  container: {
    padding: 15,
    borderWidth: 1,
    alignSelf: 'flex-start',
    borderRadius: 50,
    borderColor: colors.primary_1,
  },
});
